import { ElieLongPressDirective } from './elie-long-press.directive';

describe('ElieLongPressDirective', () => {
  it('should create an instance', () => {
    const directive = new ElieLongPressDirective();
    expect(directive).toBeTruthy();
  });
});
